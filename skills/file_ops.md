<!-- keywords: 파일, file, 읽기, 쓰기, read, write, 저장 -->
# 파일 작업 가이드

- 파일을 쓰기 전에 상위 디렉토리 존재를 신경 쓰지 않아도 된다 (write_file 이 자동 생성).
- 대용량 파일은 전체를 읽지 말고 `grep` 으로 필요한 부분만 추린다.
- 결과를 파일로 저장하라는 요청이면 `write_file` 로 저장한 뒤, 저장 경로를 보고한다.
